language: java

jdk:
  - openjdk8
  - openjdk14

install:
  - ./gradlew -version

before_script:
  - ./gradlew clean assemble

jobs:
  include:
    - stage: test
      script:
        - ./gradlew check

    - stage: test
      script:
        - ./gradlew check --no-parallel
